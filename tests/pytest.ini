[pytest]
# Tell pytest where the tests live
testpaths = tests

# Make the project root importable (so ptg_model is visible)
pythonpath = .

# Use normal Python import semantics instead of pytest's sandboxed mode
addopts = --import-mode=append
